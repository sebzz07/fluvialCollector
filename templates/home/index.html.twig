{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue sur Fluvial Collector</h1>

    <div>Retrouvez l'ensemble des revues de l'emblematique Magazine Fuvial, disponible gratuitement !</div>
</div>

    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
<div class="row justify-content-center">
    {% for magazine in magazines %}
        <div class="col-sm-4 col-lg-4 col-md-4">
            <div class="card" style="width: 18rem;">
                <div class="card-header">
                    <h5><a href="{{ asset(magazine.file) }}" class="text-decoration-none text-capitalize">Fluvial N°{{ magazine.number }}</a></h5>
                </div>
            </div>
        </div>
    {% endfor %}
    {% endif %}

{% endblock %}
