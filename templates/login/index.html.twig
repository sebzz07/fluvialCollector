{% extends 'base.html.twig' %}
{% block title %}Revue Fluvial{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <p>Vous vous trouvez actuellement sur la page d'authentification donnant accès aux archives du magazine Fluvial,
            couvrant la période du numéro 226 (octobre 2012) au numéro 319 (février 2022). Si vous n'avez pas encore de compte, vous pouvez vous
            <a href="{{ asset('/compte/register') }}">inscrire ici</a>.</p>
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" autocomplete="off">

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password">

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"> #}

        <button type="submit">login</button>
    </form>
{% endblock %}